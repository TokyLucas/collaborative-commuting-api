meta {
  name: 200 Voiture CONDUCTEUR
  type: http
  seq: 2
}

get {
  url: {{URL}}/api/users/{{CONDUCTEUR_ID}}/cars/68adbf9619417ef45894a6ee
  body: none
  auth: bearer
}

auth:bearer {
  token: {{CONDUCTEUR_TOKEN}}
}

assert {
  res.status: eq 200
}
