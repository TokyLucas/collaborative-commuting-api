meta {
  name: 200 All Voiture CONDUCTEUR
  type: http
  seq: 1
}

get {
  url: {{URL}}/api/users/{{CONDUCTEUR_ID}}/cars
  body: none
  auth: bearer
}

auth:bearer {
  token: {{CONDUCTEUR_TOKEN}}
}

assert {
  res.status: eq 200
}
