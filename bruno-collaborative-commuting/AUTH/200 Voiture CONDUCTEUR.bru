meta {
  name: 200 Voiture CONDUCTEUR
  type: http
  seq: 11
}

get {
  url: {{URL}}/api/users/{{CONDUCTEUR_ID}}/cars/
  body: none
  auth: bearer
}

auth:bearer {
  token: {{CONDUCTEUR_TOKEN}}
}

assert {
  res.status: eq 200
}
